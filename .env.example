# انسخ هذا الملف إلى .env.local
# رابط قاعدة بيانات Convex (يُستخدم في الواجهة الأمامية — عام وليس سرًّا)
VITE_CONVEX_URL=https://kindred-falcon-584.convex.cloud

# يُضاف تلقائيًا عند تشغيل `npx convex dev`:
# CONVEX_DEPLOYMENT=dev:kindred-falcon-584
